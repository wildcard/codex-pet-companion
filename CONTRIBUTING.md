# Contributing

Open an issue before changing public behavior. Keep host copy and pet artwork out of the framework-neutral engine. Every change should pass `pnpm verify`.

New fixture pets must include provenance and compatible redistribution terms. Never commit private pet photos, prompts, credentials, or machine-specific Codex state.
