# Security

Report vulnerabilities privately through GitHub Security Advisories for `wildcard/codex-pet-companion`.

The SDK does not read Codex desktop state, execute host commands, or send analytics. A configured pet manifest is fetched by the browser; treat untrusted URLs like any other remote content and constrain them with the host Content Security Policy.
